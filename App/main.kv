<MySpace>
    GridLayout:
        cols:1
        size:root.width, root.height
        GridLayout:
            cols:1
            Label:
                id: message
                text: app.gps_location
            Label:
                text: app.gps_status
            TextInput:
                id: recipient
                multiline: False
                on_text_validate: message.focus = True
            ToggleButton:
                text: 'Start' if self.state == 'normal' else 'Stop'
                on_state:
                    app.start(500000,0) if self.state == 'down' else \
                    app.stop()
            IntentButton:
                sms_recipient: recipient.text
                sms_message: app.gps_location
                text: 'Send SMS'
                size_hint_y: None
                height: sp(40)
                on_release: self.send_sms()
            